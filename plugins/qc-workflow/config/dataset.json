{
  "workflow": "qc-annotation-workflow",
  "groups": [
    {
      "key": "qc",
      "label": "External QC",
      "description": "Configure the external QC application that receives annotations after the annotate step.",
      "fields": [
        {
          "key": "endpoint",
          "label": "Endpoint URL",
          "type": "string",
          "required": true,
          "placeholder": "https://qc-app.example.com/annotations",
          "description": "URL that IDAH will POST the annotation data to."
        },
        {
          "key": "callback_token",
          "label": "Callback Token",
          "type": "password",
          "required": true,
          "placeholder": "Shared secret",
          "description": "Secret used by the external app when calling back to IDAH."
        }
      ]
    }
  ]
}