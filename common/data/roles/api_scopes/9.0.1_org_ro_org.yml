api/org_ro_org:
  title: "API - Organization Read Only Own (Org Scope)"
  mask: "9.0.1"
  description: |
    API role for read-only access to only is own organization resources.
  assignable: false
  rights:
    # Organizations
    - "iam:organizations.read.as_org_owner"
