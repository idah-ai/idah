# Until later in the development process,
# we will keep anonymous role able to do everything on the platform

anonymous:
  assignable: false
  rights:
    # - "*.*.*"

    ## user
    # dataset service
    # projects
    - "dataset:projects.*.as_user"
    # project memberships
    - "dataset:project_members.create.*" # TODO: implement a scope for this
    - "dataset:project_members.read.as_user"
    - "dataset:project_members.delete.as_user"
    # datasets
    - "dataset:datasets.*.as_user"
    # entries
    - "dataset:entries.*.as_user"
    # annotations
    - "dataset:annotations.*.as_user"

    # media service
