# Until later in the development process,
# we will keep anonymous role able to do everything on the platform

anonymous:
  assignable: false
  rights:
    # - "*.*.*"

    ## WIP: user account
    # dataset service
    # projects
    - "dataset:projects.*.as_user"
    # project memberships
    - "dataset:project_members.create.as_user" # TODO: implement a scope for this
    - "dataset:project_members.read.as_user"
    - "dataset:project_members.delete.as_user"
    # datasets
    - "dataset:datasets.*.as_user"
    # entries
    - "dataset:entries.*.as_user"
    # annotations
    - "dataset:annotations.*.as_user"

    # TODO: WIP, api checks with dataset service ?
    # media service
    - "media:medias.*.*"

    # TODO: WIP, differ between admin and user
    # iam service
    - "iam:accounts.*.*"
