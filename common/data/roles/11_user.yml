user:
  title: "User"
  description: "User"
  assignable: true
  rights:
    # dataset service
    # projects
    - "dataset:projects.*.as_user"
    # project memberships
    - "dataset:project_members.create.as_user" # TODO: implement a scope for this
    - "dataset:project_members.read.as_user"
    - "dataset:project_members.delete.as_user"
    # datasets
    - "dataset:datasets.*.as_user"
    # entries
    - "dataset:entries.*.as_user"
    # annotations
    - "dataset:annotations.*.as_user"

    # TODO: WIP
    # media service
    - "media:medias.*.*"
