# Development settings for the setting service, loaded by compose.override.yml.
# Values shared by every service are in config/development/.env.

DATABASE_URI=postgres://${POSTGRES_CREDENTIALS}@${POSTGRES_HOST}:${POSTGRES_PORT}/idah_setting_development

IDAH_SERVICE_ACCOUNT=setting@services.idah.ai
