# Development settings for the dataset service, loaded by compose.override.yml.
# Values shared by every service are in config/development/.env.

DATABASE_URI=postgres://${POSTGRES_CREDENTIALS}@${POSTGRES_HOST}:${POSTGRES_PORT}/idah_dataset_development

IDAH_SERVICE_ACCOUNT=dataset@services.idah.ai
