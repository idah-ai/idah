# Development settings for the audit service, loaded by compose.override.yml.
# Values shared by every service are in config/development/.env.

DATABASE_URI=postgres://${POSTGRES_CREDENTIALS}@${POSTGRES_HOST}:${POSTGRES_PORT}/idah_audit_development

IDAH_SERVICE_ACCOUNT=audit@services.idah.ai
